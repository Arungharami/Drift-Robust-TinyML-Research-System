name: Export Research Portal Evidence

on:
  push:
    paths:
      - configs/pipeline_stages.yaml
      - results/registry/experiment_registry.csv
      - results/xai/**
      - artifacts/explanations/**/manifest.json
      - paper/claim_evidence_matrix.csv
      - scripts/portal/export_evidence.py
      - .github/workflows/evidence-export.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  export:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: python -m pip install -r requirements.txt
      - name: Export evidence from authoritative artifacts
        run: python scripts/portal/export_evidence.py
      - name: Validate exported JSON
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          evidence = Path("research-portal/data/evidence")
          files = sorted(evidence.glob("*.json"))
          assert files
          for path in files:
              json.loads(path.read_text(encoding="utf-8"))
          pipeline = {
              stage["id"]: stage
              for stage in json.loads((evidence / "pipeline.json").read_text(encoding="utf-8"))
          }
          assert pipeline["10"]["status"] == "EXECUTED"
          assert pipeline["11"]["status"] == "EXECUTED"
          assert pipeline["12"]["status"] == "EXECUTED"
          xai = json.loads((evidence / "xai.json").read_text(encoding="utf-8"))
          assert xai["fidelity_status"] == "EXECUTED"
          assert xai["fidelity_summary"]
          assert xai["stability_status"] == "EXECUTED"
          assert xai["latency_status"] == "EXECUTED"
          embedded = json.loads((evidence / "embedded.json").read_text(encoding="utf-8"))
          assert embedded["stage15_hardware_gate"]["scientific_execution_status"] == "BLOCKED_HARDWARE"
          PY
      - name: Commit generated evidence
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add research-portal/data/evidence/*.json
          if git diff --cached --quiet; then
            echo "Portal evidence is unchanged."
          else
            git commit -m "Synchronize research portal evidence"
            git push origin "HEAD:${GITHUB_REF_NAME}"
          fi
