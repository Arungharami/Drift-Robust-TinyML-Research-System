# Table index

All publication tables remain `PLANNED`; no values are entered manually. Dataset chronology and drift tables will be derived from verified CSV artifacts. Model, latent, XAI, and quantization tables are `NOT_EXECUTED`.
