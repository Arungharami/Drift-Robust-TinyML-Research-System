# Reproducibility

Run commands from the repository root. `scripts/capture_environment.py` records software, hardware visibility, seed, and Git commit. Dataset files are downloaded from the official UCI URL and independently hashed; raw files are not committed. Validation must be `COMPLETED` before analysis. Each experiment is append-only in the registry and linked to its artifacts.
